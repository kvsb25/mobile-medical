in north_consumer.go file processMessage function to process the incoming messages in the queues


In frontend, in AmbulanceDriverDashboard.jsx establish a server sent event stream connection with the POST 'localhost:2426/ambulanceDriver/update' to continuously send driver location (using javascript geolocation api in the frontend) every minute or few minutes to the backend so that it can be produced to the kafka topic -> 'ambulances' (based on region whose code is implemented) for further consumption of the same topic by the users of role->'Patient' on NearbyAmbulances.jsx page.

In Backend, in north_consumer.go, either put the logic for consuming topic 'ambulances' in function 'processMessage' or somewhere else appropriate from where the user of role->'Patient' on the page NearbyAmbulances.jsx can get continuous updates on location of the drivers from the backend through a stream of events using server sent events.

----------------
first check ambulanceUser.GET("/stream", controllers.StreamNearbyAmbulances) implementation and NearbyAmbulances.jsx implementation whether they support server sent events. If yes how are they supporting server sent events.

In Backend, in north_consumer.go, in function 'processMessage' process the topic 'ambulances' as follows:
    - if redis entry with key template 'region:driverID' does not exist: create a redis entry following the key template with value template -> '{location:{lat:float, lng:float}}' and the value being the location in the latest message of topic 'ambulances'
    - if redis entry exits: overwrite the entry every time with the location in the latest message in the topic 'ambulances'


In Frontend, in NearbyAmbulances.jsx, 
--------------------

fix signup frontend for RBAC

====================================================================================================================

In /consumer/north_consumer.go, /consumer/sourt_consumer.go, there is no processing of the messages of topic north-ambulance or south-ambulance, and the processing to be done is putting the message into redis with geoKey and metaKey as mentioned in function getNearbyAvailableAmbulances at /controllers/ambulance.go. Make sure the values for the geoKey and metaKey correspond to how they are expected and used in getNearbyAvailableAmbulances function.


Role based sign up nahi ho raha. OTP logic check